<h1>Listing users</h1>
<table>
  <tr>
    <th>Username</th>
    <th>Email</th>
    <th colspan="3">&nbsp;</th>
  </tr>
  <%= render( { :partial => 'users/users', :object => users } ) %>
</table>
<%-
  content_for_or_javascript_tag_if( request.xhr?, :document_ready_ui ) do
-%>
  showUserButtonsHandler( jQuery );
  editUserButtonsHandler( jQuery );
  deleteUserButtonsHandler( jQuery );
<%-
  end
-%>
